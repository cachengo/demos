apiVersion: v1
kind: Service
metadata:
  name: {{ template "sso.fullname" . }}
  labels:
    app: {{ template "sso.name" . }}
    chart: {{ template "sso.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "sso.name" . }}
    release: {{ .Release.Name }}
