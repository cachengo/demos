apiVersion: v1
kind: Service
metadata:
  name: {{ template "facerec.fullname" . }}
  labels:
    app: {{ template "facerec.name" . }}
    chart: {{ template "facerec.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "facerec.name" . }}
    release: {{ .Release.Name }}
