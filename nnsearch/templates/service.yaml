apiVersion: v1
kind: Service
metadata:
  name: {{ template "nnsearch.fullname" . }}
  labels:
    app: {{ template "nnsearch.name" . }}
    chart: {{ template "nnsearch.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "nnsearch.name" . }}
    release: {{ .Release.Name }}
